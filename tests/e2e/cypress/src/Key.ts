enum Key {
    ArrowDown = '{downarrow}',
    ArrowLeft = '{leftarrow}',
    ArrowRight = '{rightarrow}',
    ArrowUp = '{uparrow}',
    Enter = '{enter}',
    Shift = '{shift}'
}

export default Key;